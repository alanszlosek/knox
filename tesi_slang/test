
	/*
	if (-1 == SLang_init_all ())
		exit (EXIT_FAILURE);

	if (-1 == SLadd_intrinsic_function ("vtCreate", (FVOID_STAR) vtCreate, SLANG_VOID_TYPE, 5, SLANG_INT_TYPE, SLANG_INT_TYPE, SLANG_INT_TYPE, SLANG_INT_TYPE, SLANG_INT_TYPE))
	       exit (EXIT_FAILURE);
	if (-1 == SLadd_intrinsic_function ("vtDestroy", (FVOID_STAR) vtDestroy, SLANG_VOID_TYPE, 1, SLANG_INT_TYPE))
	       exit (EXIT_FAILURE);
	if (-1 == SLadd_intrinsic_function ("vtRun", (FVOID_STAR) vtRun, SLANG_VOID_TYPE, 1, SLANG_INT_TYPE))
	       exit (EXIT_FAILURE);
	if (-1 == SLadd_intrinsic_function ("vtReady", (FVOID_STAR) vtReady, SLANG_VOID_TYPE, 1, SLANG_INT_TYPE))
	       exit (EXIT_FAILURE);
	if (-1 == SLadd_intrinsic_function ("sendToVT", (FVOID_STAR) sendToVT, SLANG_VOID_TYPE, 2, SLANG_INT_TYPE, SLANG_CHAR_TYPE))
	       exit (EXIT_FAILURE);

	if (-1 == SLang_load_file ("knox.sl")) {
		 SLang_restart (1);
	}
	*/

